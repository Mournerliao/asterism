# Asterism — 环境变量示例
# 复制本文件为仓库根目录的 `.env` 并填入真实值。
# `.env` 已被 .gitignore 忽略，绝不要提交任何密钥。

# Supabase（客户端）——取自 Supabase 项目的 Project Settings → API
# - VITE_SUPABASE_URL：项目 URL（形如 https://xxxx.supabase.co）
# - VITE_SUPABASE_ANON_KEY：anon/public key（公开可用，受 RLS 保护，非 service_role）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# GitHub OAuth：Client ID / Secret 不在此处配置，而是填入 Supabase 后台
# Authentication → Providers → GitHub。回调地址使用 Supabase 提供的
# https://<project-ref>.supabase.co/auth/v1/callback。service_role key 仅用于
# 服务端（Edge Functions），切勿放入任何客户端 .env。
